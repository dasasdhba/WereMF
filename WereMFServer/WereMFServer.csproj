<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PublishSingleFile>true</PublishSingleFile>
  </PropertyGroup>
  <PropertyGroup>
    <WebClientDirectory>$([System.IO.Path]::GetFullPath('$(MSBuildProjectDirectory)/../WereMFWeb'))</WebClientDirectory>
  </PropertyGroup>
  <ItemGroup>
    <Content Include="..\bots_prefer.txt" Link="bots_prefer.txt" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" Condition="Exists('..\bots_prefer.txt')" />
    <Content Include="..\bots.txt" Link="bots.txt" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" Condition="Exists('..\bots.txt')" />
  </ItemGroup>
  <Target Name="BuildWebClient" BeforeTargets="PrepareForBuild" Condition="'$(DesignTimeBuild)' != 'true'">
    <Exec WorkingDirectory="$(WebClientDirectory)" Command="npm run build" />
    <ItemGroup>
      <Content Remove="wwwroot\**\*" />
      <Content Include="wwwroot\**\*" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" />
    </ItemGroup>
  </Target>
</Project>
